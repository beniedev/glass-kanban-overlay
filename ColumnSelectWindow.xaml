<Window x:Class="DesktopOverlayBoard.ColumnSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="380"
        Height="180"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Title="Select column"
        Tag="Dialog.SelectColumnTitle">
    <Border CornerRadius="14"
            Background="#EA182230"
            BorderBrush="#46FFFFFF"
            BorderThickness="1"
            Padding="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="26" ShadowDepth="0" Opacity="0.34" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Select the default Kanban column" Tag="Dialog.SelectColumnPrompt" Foreground="{StaticResource WidgetInk}" FontWeight="SemiBold" FontSize="15"/>
                <Button Grid.Column="1" Content="x" Click="Cancel_Click" Style="{StaticResource ToolButtonStyle}" FontSize="16" Padding="8,2"/>
            </Grid>
            <ComboBox x:Name="ColumnsCombo"
                      Grid.Row="1"
                      Margin="0,18,0,14"
                      Padding="8,5"
                      Foreground="#1D2733"
                      Background="#D8FFFFFF"/>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Tag="Action.Cancel" MinWidth="78" Margin="0,0,8,0" Click="Cancel_Click" Style="{StaticResource ToolButtonStyle}"/>
                <Button Content="OK" Tag="Action.Ok" MinWidth="78" Click="Ok_Click" Style="{StaticResource ToolButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
